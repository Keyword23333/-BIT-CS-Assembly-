
### 实验目标

使用32位以上汇编语言编写一个2048小游戏。

### 实验环境

本实验环境采用 `MASM（ML.exe）`+ `MASM32 SDK` 的 `Win32 API` 宏与库文件，可以在`（x86-64）` windows系统上运行。


### 游戏规则

### 游戏框架
介绍一下每个模块的功能，比如主程序入口，GUI，逻辑控制。。。
```mermaid

```


### 实验结果
展示运行时候的一些图片

### 实验心得
